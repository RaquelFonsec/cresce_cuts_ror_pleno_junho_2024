<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Título da Página</title>
    <style>
        body {
            background-color: #fff; 
        }
        .form-bg {
            background-color: #d3d0e0; 
            padding: 20px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <div class="container mt-5">
        <div class="row justify-content-center">
            <div class="col-md-6">
                <div class="card">
                    <div class="card-body form-bg"> <!-- Aplica o estilo do fundo lilás ao formulário -->
                        <h2 class="text-center mb-4">Log in</h2>

                        <!-- Formulário de login -->
                        <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
                            <!-- Campos de email e senha -->
                        <% end %>

                        <%= render "devise/shared/links" %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Barra de navegação -->
    <div class="navbar navbar-expand-sm navbar-light" style="height: 60px; background-color: #007bff;">
      <div class="container-fluid">
        <div class="navbar-brand mx-auto" style="font-size: 20px; color: white;">
          Oferta Especial
        </div>

        <div class="ml-auto">
          <% if user_signed_in? %>
            <%= link_to "Sair", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-light" %>
          <% end %>
        </div>
      </div>
    </div>
</body>
</html>
