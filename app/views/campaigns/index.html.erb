<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Todas Campanhas</title>
  <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    .promo-message {
      font-size: 24px;
      color: #007bff; 
      text-align: center;
      margin-bottom: 20px;
    }

    .status-icon {
      margin-right: 8px;
    }

    .actions a, .actions button {
      margin-right: 10px;
      color: #007bff;
      text-decoration: none;
      transition: color 0.3s;
    }

    .actions a:hover, .actions button:hover {
      color: #0056b3;
    }

    .actions i {
      margin-right: 4px;
    }

    .pagination-links a, .pagination-links span {
      margin: 0 5px;
    }
  </style>
</head>
<body class="bg-light">
  <div class="container py-4">
    <div class="promo-message">
      Aproveite nossa campanha de descontos! Até 50% OFF em produtos selecionados.
    </div>

    <table class="table table-bordered table-hover">
      <thead>
        <tr>
          <th>Produto</th>
          <th>Descrição</th>
          <th>Preço original</th>
          <th>Preço com Desconto</th>
          <th>Status</th>
          <th>Data de Início</th>
          <th>Data de Término</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
        <% @campaigns.each do |campaign| %>
          <tr>
            <td>
              <% if campaign.image.attached? %>
                <%= image_tag campaign.image, class: "img-thumbnail", style: "max-width: 100px; max-height: 100px;" %>
              <% else %>
                Sem imagem
              <% end %>
              <br>
              <%= campaign.product.name %>
            </td>
            <td><%= campaign.description %></td>
            <td><%= number_to_currency(campaign.original_price, unit: "R$", precision: 2) %></td>
            <% if campaign.discount.present? && campaign.discount.discount_type == 'valor_fixo' %>
              <% preco_descontado = campaign.original_price - campaign.discount.discount_value.to_f %>
              <td><%= number_to_currency(preco_descontado, unit: "R$", precision: 2) %></td>
            <% else %>
              <td><%= number_to_currency(campaign.discounted_price, unit: "R$", precision: 2) %></td>
            <% end %>
            <td>
              <% if campaign.ativo? %>
                <i class="fas fa-check-circle status-icon text-success"></i>
                Ativo
              <% elsif campaign.expirado? %>
                <i class="fas fa-times-circle status-icon text-danger"></i>
                Expirado
              <% else %>
                <i class="fas fa-clock status-icon text-warning"></i>
                Agendado
              <% end %>
            </td>
            <td><%= l(campaign.start_date) %></td>
            <td><%= l(campaign.end_date) %></td>
            <td class="actions">
              <% if campaign.discount.present? %>
                <%= link_to history_discount_campaign_discount_path(campaign, campaign.discount), class: 'btn btn-outline-primary btn-sm' do %>
                  <i class="fas fa-eye"></i> Histórico
                <% end %>
              <% else %>
                <span class="btn btn-outline-secondary btn-sm">Sem desconto</span>
              <% end %>
              
              <%= link_to edit_campaign_path(campaign), class: 'btn btn-outline-warning btn-sm' do %>
                <i class="fas fa-pencil-alt"></i> Editar
              <% end %>
              
              <%= button_to campaign_path(campaign), method: :delete, data: { confirm: 'Tem certeza de que deseja excluir esta campanha?' }, class: 'btn btn-outline-danger btn-sm' do %>
                <i class="fas fa-trash-alt"></i> Excluir
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>

    <div class="pagination-links d-flex justify-content-center">
      <%= will_paginate @campaigns, container: false, class: '' %>
    </div>

    <%= link_to 'Nova Campanha', new_campaign_path, class: "btn btn-success" %>
  </div>

  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.2/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
