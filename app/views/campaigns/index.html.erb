<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todas Campanhas</title>
    <!-- Inclua o link para Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
    <style>
        body {
            background-color: black;
            color: white;
        }
        table {
            background-color: #333;
            color: white;
            width: 100%;
        }
        th, td {
            border: 1px solid white;
            padding: 8px;
            text-align: left;
        }
        .btn {
            background-color: #333;
            color: white;
            text-decoration: none;
            padding: 10px 15px;
            display: inline-block;
        }
        .btn:hover {
            background-color: #555;
        }
        .link {
            color: blue;
            text-decoration: underline;
            display: flex;
            align-items: center;
        }
        .link i {
            margin-right: 4px;
        }
        .link:hover {
            color: #00f;
        }
        .actions {
            display: flex;
            gap: 8px;
        }
        .actions form {
            margin: 0;
            padding: 0;
        }
        .actions button {
            background: none;
            border: none;
            padding: 0;
            color: blue;
            text-decoration: underline;
            display: flex;
            align-items: center;
        }
        .actions button i {
            margin-right: 4px;
        }
        .actions button:hover {
            color: #00f;
        }
        .status-icon {
            margin-right: 8px;
        }
    </style>
</head>
<body>
    <h1>Todas Campanhas</h1>

    <table class="table table-striped">
        <thead class="thead-dark">
            <tr>
                <th>Produto</th>
                <th>Preço original</th>
                <th>Preço com Desconto</th>
                <th>Status</th>
                <th>Data de Início</th> 
                <th>Data de Término</th> 
                <th>Histórico de Descontos</th>
                <th>Excluir Campanha</th>
            </tr>
        </thead>
        <tbody>
            <% @campaigns.each do |campaign| %>
                <tr>
                    <td>
                        <% if campaign.image.attached? %>
                            <%= image_tag campaign.image, class: "img-thumbnail", style: "max-width: 100px; max-height: 100px;" %>
                        <% else %>
                            No Image
                        <% end %>
                        <br>
                        <%= campaign.product.name %>
                    </td>
                    <td><%= campaign.product.price %></td>
                    <td><%= campaign.discounted_price %></td>
                    <td>
                        <% if campaign.status == 'ativo' %>
                            <i class="fas fa-check-circle status-icon" style="color: green;"></i>
                        <% elsif campaign.status == 'agendado' %>
                            <i class="fas fa-clock status-icon" style="color: orange;"></i>
                        <% else %>
                            <i class="fas fa-times-circle status-icon" style="color: red;"></i>
                        <% end %>
                        <%= campaign.calculated_status %>
                    </td>
                    <!-- Exibir as datas de início e término -->
                    <td><%= campaign.start_date %></td>
                    <td><%= campaign.end_date %></td>
                    <!-- Fim da exibição das datas de início e término -->
                    <td>
                        <%= link_to campaign_path(campaign), class: 'link' do %>
                            Ver Histórico
                        <% end %> |
                        <%= link_to edit_campaign_path(campaign), class: 'link' do %>
                            <i class="fas fa-pen"></i> Editar
                        <% end %>
                    </td>
                    <td>
                        <form action="<%= campaign_path(campaign) %>" method="post" style="display: inline;">
                            <%= csrf_meta_tags %>
                            <%= hidden_field_tag :_method, :delete %>
                            <button type="submit" data-confirm="Tem certeza que deseja excluir esta campanha?" class="link" style="background: none; border: none; padding: 0; color: blue; text-decoration: underline;">
                                <i class="fas fa-trash"></i> Excluir
                            </button>
                        </form>
                    </td>
                </tr>
            <% end %>
        </tbody>
    </table>

    <%= link_to 'Nova Campanha', new_campaign_path, class: "btn btn-success" %>
</body>
</html>
